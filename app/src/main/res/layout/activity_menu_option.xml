<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_menu_option"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.admin.ich.modules.Home.MenuTab.MenuOptionActivity">
    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/radioRecycle">

    </android.support.v7.widget.RecyclerView>
    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/radioRecycle"
        android:id="@+id/checkboxRecycle">

    </android.support.v7.widget.RecyclerView>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/frameSelect"
        android:visibility="gone"
        android:layout_marginTop="@dimen/web_size"
        android:layout_below="@id/checkboxRecycle"
        android:background="@drawable/spinner_bg"
        android:layout_weight="1">





        <TextView
            android:id="@+id/input_select"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/colorAccent"
            android:paddingStart="@dimen/web_size"
            android:hint="@string/hint_country"
            android:layout_gravity="center_vertical"
            android:inputType="text"
            android:textColor="@color/colorPrimaryText"
            android:textColorHint="@color/colorDividerLine"
            android:textSize="@dimen/text_size" />

        <Spinner
            android:id="@+id/spinnerSelect"
            android:layout_width="match_parent"
            android:layout_height="42dp"
            android:visibility="invisible">

        </Spinner>
    </FrameLayout>


<Button
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="Add to cart"
    android:textColor="#fff"
    android:background="@color/colorAccent"
    android:id="@+id/btnAddCartOptions"
    android:layout_alignParentBottom="true"/>
</RelativeLayout>
